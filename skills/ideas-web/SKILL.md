---
name: ideas-web
description: Open the ideamine dashboard in your browser, served on this machine with live data. Board, timeline (Gantt), table, projects, flow, impact matrix, groups, and search by meaning. "off" stops the server. The ideamine hook answers this with no model call.
argument-hint: "[off]"
disable-model-invocation: true
---

The ideamine hook did not answer this command, and only the hook or the ideamine CLI can start the server. Tell the user to run `ideamine serve --open` (or `ideamine serve off`) in a terminal. Then stop.
